# lib/

Place a copy of the Slay the Spire 2 managed assembly here as `sts2.dll`.

Typical Steam install path on Windows:

```
C:\Program Files (x86)\Steam\steamapps\common\SlayTheSpire2\<game data folder>\sts2.dll
```

This file is git-ignored — never commit MegaCrit's binary.
